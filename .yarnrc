# The app runs in Docker (node:22) / on Heroku (node 22.x); the host only
# launches `docker compose` via the dev scripts, so the host's Node version is
# irrelevant. Don't let yarn's engine check block running scripts on the host.
ignore-engines true
